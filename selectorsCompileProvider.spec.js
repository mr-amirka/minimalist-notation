/**
 * @overview selectors-compile-provider tests
 *
 * @author Absolutely Amir <mr.amirka@ya.ru>
 * 
 * // TODO: сделать тесты
 */

/*
import {selectorsCompileProvider} from './selectors-compile-provider';

const selectorsCompile = selectorsCompileProvider();

((classNames) => {
  const output = {};
  classNames.forEach(className => output[className] = selectorsCompile(className));
  console.log('output', output);
})([
  '(m100|p20)<.parent',
  'm(100|20)',
  '(p|x)d(xm|sm)'
]);
*/